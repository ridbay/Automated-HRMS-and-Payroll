import { Hono } from 'hono';

// Placeholder so the worker keeps bundling while the real AI routes are
// built out in a concurrent session — replace with actual handlers.
const aiRoutes = new Hono();

export default aiRoutes;
